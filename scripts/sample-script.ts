import { ethers, network } from "hardhat"

async function sampleScript() {
    // Script Body
}

sampleScript()
    .then(() => process.exit(0))
    .catch((error: any) => {
        console.error(error)
        process.exit(1)
    })
